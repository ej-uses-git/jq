HAVE_PTHREAD ? 1|undef include=pthread.h
HAVE_ALLOCA_H ? 1|undef include=alloca.h
HAVE_ALLOCA ? 1|undef include=alloca.h int main() {alloca(0);}
HAVE_ISATTY ? 1|undef include=unistd.h int main() {isatty(0);}
HAVE__ISATTY ? 1|undef include=io.h int main() {_isatty(0);}
HAVE_STRPTIME ? 1|undef include=time.h int main() {strptime(0, 0, 0);}
HAVE_STRFTIME ? 1|undef include=time.h int main() {strftime(0, 0, 0, 0);}
HAVE_SETENV ? 1|undef include=stdlib.h int main() {setenv(0, 0, 0);}
HAVE_TIMEGM ? 1|undef include=time.h int main() {timegm(0);}
HAVE_GMTIME_R ? 1|undef include=time.h int main() {gmtime_r(0, 0);}
HAVE_GMTIME ? 1|undef include=time.h int main() {gmtime(0);}
HAVE_LOCALTIME_R ? 1|undef include=time.h int main() {localtime_r(0, 0);}
HAVE_LOCALTIME ? 1|undef include=time.h int main() {localtime(0);}
HAVE_GETTIMEOFDAY ? 1|undef include=sys/time.h int main() {gettimeofday(0, 0);}
HAVE_TM_TM_GMT_OFF ? 1|undef include=time.h int main() {struct tm t; return t.tm_gmtoff != 0;}
HAVE_TM___TM_GMT_OFF ? 1|undef include=time.h int main() {struct tm t; return tm.__tm_gmtoff != 0;}
HAVE_SETLOCALE ? 1|undef include=locale.h int main() {setlocale(0,0);}
HAVE_ARC4RANDOM ? 1|undef include=stdlib.h int main() {return arc4random();}
HAVE_GETENTROPY ? 1|undef include=unistd.h int main() {getentropy(0, 0);}
HAVE_PTHREAD_KEY_CREATE ? 1|undef include=pthread.h int main() {pthread_key_create(NULL, NULL);}
HAVE_PTHREAD_ONCE ? 1|undef include=pthread.h int main() {pthread_once(NULL, NULL);}
HAVE_ATEXIT ? 1|undef include=stdlib.h int main() {atexit(NULL);}
HAVE_ACOS ? 1|undef include=math.h int main() {acos(0);}
HAVE_ACOSH ? 1|undef include=math.h int main() {acosh(0);}
HAVE_ASIN ? 1|undef include=math.h int main() {asin(0);}
HAVE_ASINH ? 1|undef include=math.h int main() {asinh(0);}
HAVE_ATAN2 ? 1|undef include=math.h int main() {atan2(0, 0);}
HAVE_ATAN ? 1|undef include=math.h int main() {atan(0);}
HAVE_ATANH ? 1|undef include=math.h int main() {atanh(0);}
HAVE_CBRT ? 1|undef include=math.h int main() {cbrt(0);}
HAVE_CEIL ? 1|undef include=math.h int main() {ceil(0);}
HAVE_COPYSIGN ? 1|undef include=math.h int main() {copysign(0, 0);}
HAVE_COS ? 1|undef include=math.h int main() {cos(0);}
HAVE_COSH ? 1|undef include=math.h int main() {cosh(0);}
HAVE_DREM ? 1|undef include=math.h int main() {drem(0);}
HAVE_ERF ? 1|undef include=math.h int main() {erf(0);}
HAVE_ERFC ? 1|undef include=math.h int main() {erfc(0);}
HAVE_EXP10 ? 1|undef include=math.h int main() {exp10(0);}
HAVE_EXP2 ? 1|undef include=math.h int main() {exp2(0);}
HAVE_EXP ? 1|undef include=math.h int main() {exp(0);}
HAVE_EXPM1 ? 1|undef include=math.h int main() {expm1(0);}
HAVE_FABS ? 1|undef include=math.h int main() {fabs(0);}
HAVE_FDIM ? 1|undef include=math.h int main() {fdim(0, 0);}
HAVE_FLOOR ? 1|undef include=math.h int main() {floor(0);}
HAVE_FMA ? 1|undef include=math.h int main() {fma(0, 0, 0);}
HAVE_FMAX ? 1|undef include=math.h int main() {fmax(0, 0);}
HAVE_FMIN ? 1|undef include=math.h int main() {fmin(0, 0);}
HAVE_FMOD ? 1|undef include=math.h int main() {fmod(0, 0);}
HAVE_FREXP ? 1|undef include=math.h int main() {frexp(0, 0);}
HAVE_GAMMA ? 1|undef include=math.h int main() {gamma(0);}
HAVE_HYPOT ? 1|undef include=math.h int main() {hypot(0, 0);}
HAVE_J0 ? 1|undef include=math.h int main() {j0(0);}
HAVE_J1 ? 1|undef include=math.h int main() {j1(0);}
HAVE_JN ? 1|undef include=math.h int main() {jn(0, 0);}
HAVE_LDEXP ? 1|undef include=math.h int main() {ldexp(0, 0);}
HAVE_LGAMMA ? 1|undef include=math.h int main() {lgamma(0);}
HAVE_LOG10 ? 1|undef include=math.h int main() {log10(0);}
HAVE_LOG1P ? 1|undef include=math.h int main() {log1p(0);}
HAVE_LOG2 ? 1|undef include=math.h int main() {log2(0);}
HAVE_LOG ? 1|undef include=math.h int main() {log(0);}
HAVE_LOGB ? 1|undef include=math.h int main() {logb(0);}
HAVE_MODF ? 1|undef include=math.h int main() {modf(0, 0);}
HAVE_LGAMMA_R ? 1|undef include=math.h int main() {lgamma_r(0);}
HAVE_NEARBYINT ? 1|undef include=math.h int main() {nearbyint(0);}
HAVE_NEXTAFTER ? 1|undef include=math.h int main() {nextafter(0, 0);}
HAVE_NEXTTOWARD ? 1|undef include=math.h int main() {nexttoward(0, 0);}
HAVE_POW ? 1|undef include=math.h int main() {pow(0, 0);}
HAVE_REMAINDER ? 1|undef include=math.h int main() {remainder(0, 0);}
HAVE_RINT ? 1|undef include=math.h int main() {rint(0);}
HAVE_ROUND ? 1|undef include=math.h int main() {round(0);}
HAVE_SCALB ? 1|undef include=math.h int main() {scalb(0, 0);}
HAVE_SCALBLN ? 1|undef include=math.h int main() {scalbln(0, 0);}
HAVE_SIGNIFICAND ? 1|undef include=math.h int main() {significand(0);}
HAVE_SCALBN ? 1|undef include=math.h int main() {scalbn(0, 0);}
HAVE_ILOGB ? 1|undef include=math.h int main() {ilogb(0);}
HAVE_SIN ? 1|undef include=math.h int main() {sin(0);}
HAVE_SINH ? 1|undef include=math.h int main() {sinh(0);}
HAVE_SQRT ? 1|undef include=math.h int main() {sqrt(0);}
HAVE_TAN ? 1|undef include=math.h int main() {tan(0);}
HAVE_TANH ? 1|undef include=math.h int main() {tanh(0);}
HAVE_TGAMMA ? 1|undef include=math.h int main() {tgamma(0);}
HAVE_TRUNC ? 1|undef include=math.h int main() {trunc(0);}
HAVE_Y0 ? 1|undef include=math.h int main() {y0(0);}
HAVE_Y1 ? 1|undef include=math.h int main() {y1(0);}
HAVE_YN ? 1|undef include=math.h int main() {yn(0, 0);}
